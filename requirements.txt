# Kudos – Dependencias del proyecto.
# Instalación: pip install -r requirements.txt

# ===== Núcleo (necesario en local y producción) =====
Django>=4.2,<5.1
python-dotenv>=1.0.0

# ===== Producción =====
# Servidor WSGI rápido para Render/Railway/Heroku/VPS
gunicorn>=21.0.0
# Sirve archivos estáticos sin necesidad de nginx
whitenoise>=6.5.0
# Lee la URL de la base de datos PostgreSQL (Render, Heroku, etc.)
dj-database-url>=2.1.0
# Driver PostgreSQL (sólo se activa si DATABASE_URL existe)
psycopg2-binary>=2.9.9

# ===== Generación de PDFs (documentos estratégicos) =====
reportlab>=4.0.0
markdown>=3.5

# ===== Opcionales (descomentar si las usas) =====
# openai>=1.0.0          # Asistente con GPT real
# Pillow>=10.0.0         # Para subir imágenes
# requests>=2.31.0       # Llamadas HTTP externas
# celery>=5.3            # Tareas en segundo plano
# redis>=5.0             # Backend de Celery
