# KUDOS Engine + Capsule Engine v2 · dependencias Python
# Instalar con: pip install -r kudos_engine/requirements.txt

# ─── API v2 (FastAPI + Pydantic) ──────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-multipart>=0.0.6

# ─── Núcleo legacy ────────────────────────────────────────────────
requests>=2.31.0
pyyaml>=6.0
Pillow>=10.0.0

# ─── Voz gratis ───────────────────────────────────────────────────
edge-tts>=6.1.0

# ─── Anthropic Claude (guion) ─────────────────────────────────────
anthropic>=0.40.0

# ─── T1.2 Postgres Foundation ─────────────────────────────────────
sqlalchemy>=2.0.25
alembic>=1.13.0
psycopg2-binary>=2.9.9             # sync driver para alembic + scripts
asyncpg>=0.29.0                    # async driver para FastAPI runtime
greenlet>=3.0.0                    # required by SQLAlchemy async

# ─── T1.2 Testing critico ─────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                      # async client tests FastAPI

# ─── T1.3 OAuth + JWT ─────────────────────────────────────────────
python-jose[cryptography]>=3.3.0   # JWT encode/decode (HS256)
authlib>=1.3.0                     # Google id_token verification + JWKS
cryptography>=41.0.0               # required by python-jose

# ─── T1.5 Security middleware + observabilidad ────────────────────
slowapi>=0.1.9                     # rate limiting FastAPI
redis>=5.0.0                       # backend rate limit + cache (opcional)
sentry-sdk[fastapi]>=2.0.0         # error tracking

# OPCIONALES (descomenta si los necesitas)
# elevenlabs>=1.0.0
# openai>=1.50.0
