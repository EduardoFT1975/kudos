/**
 * @deprecated · Removed in reduction pass.
 *
 * TemporalResonance era redundante con PageAtmosphere warm glow stage 1
 * (que ya hace el trabajo de dwell warmth via useIdleDwell). Mantener
 * dos sistemas haciendo el mismo trabajo emocional es sobreingeniería.
 *
 * Si necesitas warmth en dwell: ajusta PageAtmosphere warm glow stage 1
 * en `experience/components/capsule/PageAtmosphere.tsx`.
 *
 * Este archivo puede eliminarse del disco cuando se haga limpieza.
 */
export {};
