/**
 * KUDOS Auth.js v5 · Route Handler.
 * Maneja todos los endpoints de NextAuth (/api/auth/signin, /callback, /signout, ...).
 */
import { handlers } from "@/auth";

export const { GET, POST } = handlers;
