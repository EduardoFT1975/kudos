# set_admin_access.py
import os
import sys
import django

# Añadir el directorio raíz del proyecto al sys.path
project_root = os.path.abspath(os.path.join(os.path.dirname(__file__), '.'))
sys.path.append(project_root)

# Configurar Django
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'kudos_project.settings')
django.setup()

# Importar modelos
from kudos_app.models import User, AdminAccess

# Crear entrada en AdminAccess para 'admin-test-uid-2'
try:
    user = User.objects.get(uid='admin-test-uid-2')
    admin_access, created = AdminAccess.objects.get_or_create(
        user=user,
        defaults={'access_level': 5}  # Nivel de acceso más alto
    )
    if not created and admin_access.access_level < 5:
        admin_access.access_level = 5
        admin_access.save()
    print(f"Permisos de administrador asignados a {user.alias} con access_level={admin_access.access_level}.")
except User.DoesNotExist:
    print("Error: Usuario 'admin-test-uid-2' no encontrado.")
    sys.exit(1)
except Exception as e:
    print(f"Error al asignar permisos de administrador: {e}")
    sys.exit(1)